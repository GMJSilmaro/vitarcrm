import { Briefcase, CardList, ChatLeftText, EnvelopePaper } from 'react-bootstrap-icons';

export const NOTIFICATION_ICON_MAP = {
  job: Briefcase,
  'job-request': EnvelopePaper,
  calibration: CardList,
  default: ChatLeftText,
};
